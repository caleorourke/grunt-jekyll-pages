# * _config.yml
# * Stores static configuration and project info
#
# * Spaces only, no tabs
# * Wrap words in quotes wherever commas are used

org:        Jekyll-Hyde Enterprise
permalink:  pretty
exclude:    [".gitignore",
            "Gemfile"]
gems:
  - jekyll-seo-tag

theme: jekyll-theme-minimal